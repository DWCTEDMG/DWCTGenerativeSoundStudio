<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="EdmgStudio.WinUI.Controls.JobProgressCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    AutomationProperties.Name="Global render status">
    <Border Style="{StaticResource StudioCardStyle}" Padding="12">
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border Width="48" Height="48" Background="{ThemeResource StudioLogoPanelBrush}" CornerRadius="8">
                <FontIcon HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22" Glyph="&#xE714;" />
            </Border>
            <Grid Grid.Column="1" RowSpacing="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock x:Name="TitleText" FontWeight="SemiBold" Text="Render in progress" TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="StageText" Grid.Row="1" Foreground="{ThemeResource StudioMutedTextBrush}" Text="Preparing" TextTrimming="CharacterEllipsis" />
                <Grid Grid.Row="2" ColumnSpacing="10">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <ProgressBar x:Name="JobProgress" Height="4" Minimum="0" Maximum="100" />
                    <TextBlock x:Name="EtaText" Grid.Column="1" FontSize="12" Foreground="{ThemeResource StudioMutedTextBrush}" Text="ETA calculating" />
                </Grid>
            </Grid>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <Button x:Name="PauseButton" AutomationProperties.Name="Pause render" Click="PauseButton_Click" ToolTipService.ToolTip="Pause render"><SymbolIcon Symbol="Pause" /></Button>
                <Button x:Name="CancelButton" AutomationProperties.Name="Cancel render" Click="CancelButton_Click" ToolTipService.ToolTip="Cancel render"><SymbolIcon Symbol="Cancel" /></Button>
                <Button AutomationProperties.Name="Open render queue" Click="OpenQueueButton_Click" Content="Open Queue" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
