## Archived UI Prototypes

These `.tsx` files are preserved as historical standalone UI explorations.

They are not part of the canonical EDMG Studio product build, release path, or
runtime entrypoints. The authoritative desktop product lives under:

- `studio/edmg-studio/`

Keep them here only as reference material for future design or workflow ideas.
